#include "main.h"
/**
 * rev_string - reverse a string
 *@s: a pointer to a string
 * Return: nothing
 */

void rev_string(char *s)
{
	char str[];
	int i;
	int x;

	for (i = 0; *s != 0, i++)
	{
		s++;
	}

	for (x = 0, x <= i, x++)
	{
		str[x] = *s;
	}
}
