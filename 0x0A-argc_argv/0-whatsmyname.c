#include <stdio.h>
/**
 * main - C program entry point
 * @argc: integer for counting arguments
 * @argv: an array of chars to save the arguments
 * Return: 0 if the program runs correctly
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
